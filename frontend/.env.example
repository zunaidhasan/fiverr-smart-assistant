# Backend API URL for production (Vercel) deployment
# In dev, Vite proxies /api to localhost:8000 automatically.
# In production, set this to your Render backend URL:
VITE_API_URL=https://sardarit-portfolio-api.onrender.com
